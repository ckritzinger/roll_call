SUPABASE_URL=
SUPABASE_KEY=
NUXT_PUBLIC_POWER_SYNC_URL=

# scripts/powersync-keepalive.ts — credentials for a real (non-admin-only) login,
# used to open a PowerSync client connection so the free-tier instance doesn't
# get deprovisioned for inactivity.
KEEPALIVE_EMAIL=
KEEPALIVE_PASSWORD=
